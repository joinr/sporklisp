'Allows an object to reset itself to a clean state, saving on computation and allocation.
'In some cases, we might have a more effecient way to reset the object, rather than wipe it out
'and wast time constructing.
Option Explicit

Public Sub reset()
End Sub

Public Sub Terminate()
End Sub